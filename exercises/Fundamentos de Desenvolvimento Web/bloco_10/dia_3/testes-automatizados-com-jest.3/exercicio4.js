const funcaoUm   = (str) => str.toUpperCase();
const funcaoDois = (str) =>  str.charAt(0);
const funcaoTres = (str1, str2) => str1.concat(str2);

module.exports = {
  funcaoUm,
  funcaoDois,
  funcaoTres,
};